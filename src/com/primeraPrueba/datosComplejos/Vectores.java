package com.primeraPrueba.datosComplejos;
import java.io.*;
import java.math.BigDecimal;
import java.util.ArrayList;
import java.util.Vector;

public class Vectores {
    /** los vectores se duplican cuando el array está completo y metemos otro dato
     *  Cuidado ya que un dato más puede duplicar otro vector entero (capacity x2)
     **/
    //Con vector.trimToSize(); -->podemos ajustar el tamaño del vector al actual por si ha crecido demasiado
    public static void main(String[] args) {

      /*  Vector<Integer> vector = new Vector(); //Sus parametros son inicialCapacity:  y capacityIncrement:
        vector.add(1);                         //De cuanto empieza          y           cuanto va aumentando
        vector.add(2);
        vector.add(3);

        for(Integer vectores : vector){
            System.out.println(vectores);
        }
        for(int i = 0; i < vector.size(); i++) {
            System.out.println(vector.get(i));
        }

        vector.remove(2); //elimina un vector. ELIMINA LA POSICIÓN EN MEMORIA

        System.out.println("Vector tamaño: " + vector.size() + " y capacidad: " + vector.capacity());
                            //tamaño del array subyacente               // capacidad total

        Vector<Integer> vector2 = new Vector();
        vector2.add(1);
        vector2.add(2);
        vector2.add(3);

        boolean resultado = vector.equals(vector2);
        System.out.println("Son iguales?" + resultado); */



    }

}

package com.primeraPrueba.datosComplejos;
import java.util.ArrayList;

public class ArrayLists {
    /**
     * Los ArrayList crean nuevos arrays cuando están llenos pero del 50% del anterior
     * Si el prim tenía 50 slots, el dato 51 crearía un ArrayList de 25 slots (50/2).
     *
     * ArrayList para programación secuencial (ejecuta de 1 en 1)
     * Vector para programación concurrente (ejecuta "a la vez" varias cosas)
     */

/*    ArrayList<String> lista = new ArrayList<String>();
        lista.add("Me");  //se añade igual que en vector
        lista.add("importa");
        lista.add("poco");

        lista.remove("poco"); // Se elimina igual igual que vector

     for(String listas : lista){
        System.out.println(listas.get(listas));
    }

    ArrayList<String> lista = new ArrayList<String>();
        lista.add("Me");  //se añade igual que en vector
        lista.add("importa");
        lista.add("poco");

        lista.remove("poco"); // Se elimina igual igual que vector

        for(String nombre : lista) {
        System.out.println(nombre);
    }
        for(int i = 0; i< lista.size(); i++){
        System.out.println("Qué? " + lista.get(i));
    }

    //Convertir ArrayList en Array
    String array[] = new String[lista.size()]; //creamos Array con tamaño == a ArrayList
        for(int i = 0; i < lista.size(); i++){         //Pasarle de parámetro [VAR ARRAYLIST.size()]
        array[i] = lista.get(i);  //Al array creado pasarle cada iterración el valor get(i)
    }               */

    /**
     * LinkedList modifica los datos de un Array más rápido (x ej: usuarios que entran o salen)
     * ArrayList > rapido para leer datos de Array
     */

   // LinkedList<String> listaEnlazada = new LinkedList<String>();
}

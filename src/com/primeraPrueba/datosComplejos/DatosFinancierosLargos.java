package com.primeraPrueba.datosComplejos;

import java.math.BigDecimal;

public class DatosFinancierosLargos {

    /**
     * Para datos financieros o núm grandes con decimales, hay que usar BIG DECIMAL
     */
/*
    BigDecimal valor1 = new BigDecimal(5); //Declarar tipo de dato BigDecimal y pasarle el valor en parámetro
    BigDecimal valor2 = new BigDecimal(6); // Para operar necesitamos crear los valores, no se inyectan sin más.

    BigDecimal resultado = valor1.multiply(valor2);   // Para operar debemos usar métodos
    //Creamos var con el resultado <-- le metemos el valor con la operación

        System.out.println(resultado);          */
}
